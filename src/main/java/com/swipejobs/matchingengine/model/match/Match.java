package com.swipejobs.matchingengine.model.match;

import java.util.List;
import java.util.Date;
import java.util.List;

import com.swipejobs.matchingengine.model.address.Location;
import com.swipejobs.matchingengine.model.address.SearchAddress;
import com.swipejobs.matchingengine.model.availability.Availability;
import com.swipejobs.matchingengine.model.job.Job;

public class Match {
	private Long userId;
	
	private List<Job> top3Jobs;
	
	if (currentWorker.isActive){	
		if (job.requiresLicense && currentWorker.hasLicense){
					
				}
		ArrayList array = CollectionUtils.retainAll(currentWorker, job.requiredCertifications);
		if (array.size == job.requiredCertifications.size){
			if (job.location ISCLOSE to worker.jobsearchrequest.location + maxDistance){
			
				

			}
		}
	}
	}

}
